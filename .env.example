# Public URL where the gateway is reachable (used for HLS proxy URLs)
PUBLIC_URL=http://localhost:8787

# Comma-separated origins for CORS, or * for dev
CORS_ORIGIN=*

# AWS Lightsail instance public IP or domain (set after deploy)
# PUBLIC_URL=http://YOUR_LIGHTSAIL_IP:8787
